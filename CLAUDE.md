# CLAUDE.md

Worktrees for this repo go in `.agents/worktrees/<name>` — never `/tmp`, a session
scratchpad, or `.claude/worktrees/`. Create them with `git worktree add`, enter them
with `EnterWorktree`'s `path` parameter, and remove them when the work is done.

Full instructions, including cleanup and the reasoning:

@AGENTS.md
